import 'package:flutter/material.dart';

//Se rellena con el color verde
class ProfileIncidents extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      color: Colors.greenAccent,
    );
  }
}
